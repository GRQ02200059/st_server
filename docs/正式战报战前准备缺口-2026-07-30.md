# 正式战报战前准备阶段缺口

## 对比样本

- 正式完整战报：`assent/cfg/paper/11/cap_20260312014510506_0000000b_zlib.json`
- 当前实现：从同一正式样本重建六武将阵容，以 `FixedBattleRandom(0)` 结算并由
  `ClientBattleTextReplayAdapter` 生成战报。
- 正式样本首回合 `091` 前有 625 条动作、83 种动作族；2026-07-31 当前实现为
  291 条、44 种动作族，44 种都能在正式样本中找到。
- `paper/11` 与 `paper/6231` 的 `report` 都是明文 `#` 分隔动作串，外层 JSON 已由抓包工具解压。

## 2026-07-31 实施状态

本轮已经把可由正式样本稳定验证的结构补入实现：

- 静态准备来源已经保留 `SYSTEM/ARMY/TROOP/EQUIPMENT` provenance，并能投影为
  `0k/5s/7x/ba`。
- 每个静态来源使用正式样本中的 `66 ... 67 ... 61` 内层包络。
- 战前事件使用独立 round-zero 投影器；战中的 `5x/5y/8d/1o`
  （技能开始、结束、施放、伤害）不再被挪用到战前段。
- 装备特性 `45xxxx` 按正式样本稳定形状投影为
  `8x位置,特性ID,位置,武将初始战法ID,等级`。主样本的 `8x` 已达到正式/生成 `2/2`。
- 通用 `ja` 只接受已验证的 `521–524/531–534` 效果；
  通用 `8c` 只接受已验证的派生战法区间，未知形状改为诊断而不是猜测编码。
- 已建立正式样本回归夹具：从 `0e/5p/ba/8x` 重建阵容，固定随机数结算，
  校验确定性、战前/战中包装隔离、`61/66/67`、`8x` 及共有动作参数宽度。

当前主样本已经没有生成侧独有动作族。尚缺 39 个正式动作族，绝大多数是
展示元数据、系统/赛季段和装备专用动作。共有动作的参数宽度没有发现不兼容。

## 初轮确认的缺口（保留作历史基线）

### 1. 武将初始化数据不完整

正式 `5p`（十进制 205）记录包含：

`位置,等级,初始兵力,三组战法ID/等级,两组兵种特性,装备槽及装备等级等扩展字段`

当前实现只写：

`位置,等级,初始兵力,三组战法ID/固定等级1,0,0`

因此缺少真实战法等级、兵种特性、装备/宝物数据以及其他尾部扩展字段。

### 2. 六名武将的初始属性展示完全缺失

正式样本在通用加成阶段按双方逐将输出攻击、防御、谋略、速度等属性变化及变化后的数值，例如 `19/1a/1b/1c` 动作族。当前协议表没有这些动作，适配器也从未输出 `BattleHero.stats`。

### 3. 通用战前加成流水缺失

正式样本在被动与指挥战法前还有多段初始化：

- `0k + 295xxx`：通用战斗规则/基础加成；
- `5s + 291xxx`：阵营或组合类加成；
- `7x + 296xxx`：兵种、阵营、赛季等附加效果；
- `ba + 400xxx/450xxx`：装备及装备特性。

这些阶段会逐目标写入属性、增减伤、状态、持续回合或层数。当前战报没有对应阶段；Builder 中即使已经把一部分静态加成算进最终属性，客户端也看不到“由什么带来多少变化”。

### 4. 被动、指挥、主动动作码映射错误

正式样本显示：

- `0l`（21）：被动战法；
- `0m`（22）：指挥战法；
- `0n`（23）：主动战法；
- `0o`（24）：追击动作族。

当前实现把被动和指挥都编码为 23，把主动编码为 22；这会让战前准备的战法类型和展示阶段错位。

### 5. 指挥/被动战法只报“发动”，不报完整附属值

当前 `StatChanged` 只输出一次 `skillCast(target, skillId)`，丢弃：

- 属性类型；
- 增减值；
- 变化后的属性值；
- 持续回合；
- 来源与目标之间的归属。

`StatusApplied` 也没有完整输出 `power`、`durationRounds`、`statDelta`。正式样本会继续输出 `ja`、`0s/0t`、`1x` 等动作，携带效果 ID、目标、数值、回合或层数。

### 6. 派生战法和根战法关系丢失

正式战报会区分主战法与 `210xxx/211xxx/212xxx/213xxx` 派生效果。引擎事件虽然有 `rootSkillId`，适配器只使用 `skillId`，无法完整还原“战法发动—附属效果—目标变化”的层级。

### 7. 效果生命周期表达不完整

当前仅覆盖少量状态施加、移除和阻挡动作。以下内容没有完整投影：

- 效果冲突、覆盖与刷新；
- 层数变化和最大层数；
- 增伤、减伤、受到伤害变化的具体百分比；
- 规避、洞察、先手、连击等概率或持续值；
- 装备特性、兵种特性及阵营加成的来源；
- 战前目标选择和被影响目标列表。

遇到未支持效果时，安全模式只记录服务端 warning，并从客户端战报中省略，因此客户端看到的准备阶段会比真实结算少。

## 建议补全顺序

1. 先建立正式的战前阶段模型，保留战斗开始时每名武将的原始属性快照和每次效果应用后的数值。
2. 修正 `21/22/23/24` 四类战法动作映射，严格区分被动、指挥、主动、追击。
3. 完整实现 `5p` 武将信息：真实战法等级、兵种特性、装备及特性。
4. 补齐属性变化动作：来源、目标、属性/效果 ID、变化值、变化后值、持续回合和层数。
5. 再补通用规则、阵营/组合、兵种、装备四个战前阶段。
6. 最后按正式样本补效果覆盖、冲突、移除和特殊赛季动作。

在这些完成前，当前回放可以表达基础战斗过程，但不能视为正式战报的完整战前准备回放。

## 本轮已补全

- `5p` 已扩展为固定 19 字段，支持三组战法及等级、两组兵种特性、三组装备及等级；玩家和 NPC 战法等级已接入现有业务中的 10 级数据。
- 战斗结果保留双方入场快照，初始化字段不再误用战斗结束后的属性或兵力。
- 六名武将会输出攻击、防御、谋略、速度初始值。
- 被动、指挥、主动、追击分别使用 `0l/0m/0n/0o`。
- 属性增加使用 `19/1a/1b/1c`，属性降低使用 `1g/1h/1i/1j`，包含来源、战法、目标、持续、变化量和应用后值。
- `ja` 已按正式样本输出效果强度，不再误写持续回合。
- 成功施加的战前状态使用 `0s`；属性型 `StatusApplied` 不再与完整属性变化重复输出。
- 根战法继续使用四类发动动作，`210xxx/211xxx/...` 派生效果改用 `8c`。
- JSON 战报保留 `StatChanged.effectId/valueAfter`、`ModifierApplied`、`StatusApplied.power/statDelta`。
- 已增加真实引擎集成用例，覆盖魏武之世、大赏三军、神兵天降在首回合前的完整字段。

## 仍待业务数据或更多样本确认

- 玩家状态目前没有兵种特性、装备槽及装备等级的持久化来源，因此字段结构已支持，但真实玩家战报仍会以空值填充这些槽。
- Builder 已保留一部分阵营、营地、兵种和装备静态来源，并能生成
  `0k/5s/7x/ba`；尚未覆盖正式样本全部 `295xxx/296xxx/281xxx` 来源及其专用动作。
- 效果覆盖、刷新、冲突、层数及部分赛季特殊动作仍缺少统一的战前事件模型。
- `ja` 在主样本中仍为正式/生成 `25/75`，说明引擎产生的修饰器数量或目标展开规则
  与正式服不同；不能仅靠动作编码层消除。
- `StatusApplied` 已增加可空配置效果 ID，完整技能引擎会从持久效果规则透传。
  主例中的 `DISARM/HESITATION` 现分别生成 `0s...,752` 与 `0s...,702`，不再生成
  `0t...,552/502`。`0t` 的冲突、抵抗或未生效生命周期仍待更多证据确认。
- 攻城和攻击距离没有在已核实样本中确认与当前协议动作的一一对应关系，暂未冒充四维属性输出。

## 2026-07-31 进展：主样本 `ja` 已由 75 收敛到 23

新的真实战报 fixture 对 `ja` 使用完整五元组
`来源位置,来源ID,目标位置,效果ID,强度` 验收，而不是只比较动作族或参数宽度。

已完成：

- round 0 禁止执行 `201006` 的受伤后派生修饰器，准备期不再产生 56 条
  `223006`，且 round 1 的真实受伤叠加行为保持通过；
- 从客户端兵种特性根源 `296106/296206` 按队内站位展开
  `296132/296232`，补齐正式样本的四条 `531/533,+8`；
- fixture 固定校验正式 25 条、生成 23 条、`223006` 为 0、四条兵种
  tuple 精确存在，并验证两次固定随机结算完全一致。

当前剩余的 2 条数量差和全部技能 tuple 值差只来自
`200198/200204/200773` 的目标选择与强度计算。下一轮应直接修正这三个技能的
解释器语义；不能在客户端投影器中补目标、改符号或截断数量。
